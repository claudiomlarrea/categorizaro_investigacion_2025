# Valorador de CV Docente – UCCuyo (DOCX + PDF)
1. Instalar dependencias:
   pip install streamlit python-docx pandas pdfplumber
2. Ejecutar:
   streamlit run app.py
3. Subir un CV en formato .docx o .pdf (con texto).